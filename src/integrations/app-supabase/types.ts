// Re-exports the database typings. The schema in your own Supabase project
// must match this shape — apply db/schema.sql when first connecting it.
export type { Database, Json } from "../supabase/types";
